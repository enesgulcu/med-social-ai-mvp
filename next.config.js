/** @type {import('next').NextConfig} */
const nextConfig = {
  // Türkçe yorum: Next.js konfigürasyonu; production-ready MVP için minimal ayarlar.
  reactStrictMode: true,
  swcMinify: true,
};

module.exports = nextConfig;

